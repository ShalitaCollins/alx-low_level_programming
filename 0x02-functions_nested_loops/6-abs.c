#include "main.h"
/**
 * _abs - computes the absolute value of an integrer
 *
 * @n: the int to check
 *
 * Return: the absolute value of int
 *
 */
int _abs(int p)
{
	if (p >= 0)
	{
		return (p);
	}
	return (-p);
}
